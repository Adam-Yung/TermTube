# TermTube — Python dependencies
# Install via:  bash setup.sh
# Or manually:  pip install -r requirements.txt

# Core
PyYAML>=6.0

# TUI framework
textual>=0.68.0
rich>=13.4.0

# High-quality terminal image rendering (TGP / sixel / halfcell)
# Gracefully optional — falls back to chafa symbol art if not installed.
textual-image>=0.12.0
Pillow>=10.3.0

# YouTube data extraction (library, not CLI binary)
yt-dlp>=2024.0
yt-dlp-ejs>=0.8.0

# Windows IPC (named pipes) — only needed on Windows
pywin32>=306; sys_platform == "win32"

# System tools managed by bootstrap.py (downloaded into ~/.local/termtube-deps/bin/):
#   deno        JavaScript runtime for yt-dlp YouTube challenge solving
#   ffmpeg      audio/video muxing and format conversion
#   mpv         media playback via IPC
