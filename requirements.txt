# TermTube — Python dependencies
# Install via:  bash setup.sh
# Or manually:  pip install -r requirements.txt

# Core
PyYAML>=6.0
requests>=2.31

# TUI framework
textual>=0.68.0
rich>=13.4.0

# High-quality terminal image rendering (TGP / sixel / halfcell)
# Gracefully optional — falls back to chafa symbol art if not installed.
textual-image>=0.12.0
Pillow>=10.3.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Windows IPC (named pipes) — only needed on Windows
pywin32>=306; sys_platform == "win32"

# System tools expected on PATH (install via brew / apt / winget, not pip):
#   yt-dlp      brew install yt-dlp       / winget install yt-dlp.yt-dlp
#   mpv         brew install mpv          / winget install mpv.net
#   chafa       brew install chafa        (optional, Unix only — skipped on Windows)
#   ffmpeg      brew install ffmpeg       / winget install Gyan.FFmpeg (optional)
