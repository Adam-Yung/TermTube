** TODOS **
* suggest other UI, UX and performance and stability improvements to my terminal youtube app.  Raise any concerns with me, suggest fixes and workarounds, and discuss them with me, rewriting/updating the roadmap with me, and then we'll implement them one by one.
*Add a popup centerstage in the main termtube window when watching video in mpv, showing the video progress and the text "Playing Video in MPV Window", also allow pressing q in the termtube window to stop the mpv video.  Doing that will close the popup
* add a 'y' button shortcut to copy video url.
* when already listening to a video track, add a shortcut with 'e' to allow user to queue the currently focused video (provided it's different from the one now playing) for playing once the current audio finishes.  Add > for skip to next track if there's a video in queue, show those shortcuts in the action window or now playing window when they are available.


** Tentatively Completed **
1. in home page, I want to implement a system to eliminate stale home page feeling with the cache:
having a cache for the home page is crucial, but I want videos to be removed from the cache once they've been focused (shown in the details panel) for more than 3 times.  remove it from cache and don't show it in home page anymore.  maybe keep a dictionary (max 1000 entires to preserve memory) of these filtered videos so we don't show them even if they show up on new fetches of the home page from yt-dlp.  also, don't show watched videos in home page anymore
1. fetch new home page recommendations with yt-dlp every ten minutes to keep things fresh and save the active home page recommendations to a file in $TMPDIR/TermTube, and have new startups look for home page file in that folder first even if it's old, for quick startup times, and then fetch and update new videos for home in the background.  this way, only a cold reboot of the system will cause TermTube to take time to refresh the Home Screen.
2. in general the points above should create a seamless, quickly loading, no stale entries and responsive home page experience at all times.
3. in setup script, for python venv option, create virtual environment in $HOME/.local/share/TermTube/ and not scriptdir for portability, and prompt user to approve or skip install termtube to $HOME/.local/bin or not (check if that is in path or not as well, and handle creation if .local is missing). For installation, copy the necessary parts of the project to $HOME/.local/share/TermTube/ as well
4. update Readme and Claude.md to reflect current project status and help claude or other AI agent to actually understand and work on the project better with less token usage
5. add memory/ folder and update claude with instructions to document actions done by AI agent to memory, and to always update memory and files like README when necessary.
6. add beautiful modern looking animated  loading UI whenever we are fetching videos (on home screen when no cached results or TMPDIR file is empty, or in search page etc.) to populate the list in the left video list panel
7. reimagine the top header to be more useful and beautiful, also remove the images tgp text, and turn the clock into a green/yellow dot showing token is valid or not.  add a field in the settings page to create new token with the yt-dlp command
8. add settings ',' shortcut key next to quit in the bottom, and remove the watch and listen hints since they're covered in the action window. just show search, refresh, settings and quit
